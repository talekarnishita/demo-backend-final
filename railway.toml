# Railway config for Strapi backend (monorepo: set Root Directory to "backend" in dashboard).
# See docs/09-railway-deployment.md.

[build]
buildCommand = "npm install && npm run build"
watchPaths = ["**"]

[deploy]
startCommand = "npm run start"
